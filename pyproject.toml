[project]
name = "classification-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click",
    "numpy",
    "pandas",
    "scipy",
    "altair",
    "vl-convert-python",
    "pytest",
]
